# Bot Configuration
HEADLESS=true
FROM_CHAIN=Ethereum
TO_CHAIN=Arbitrum
TOKEN=ETH

# Amount Configuration (in ETH)
MIN_AMOUNT=0.01
MAX_AMOUNT=0.02

# Timing Configuration (in milliseconds)
DELAY=3600000      # 1 hour between transactions
RETRY_DELAY=300000 # 5 minutes retry delay
MAX_RETRIES=3

# Optional: Add wallet configuration if needed
# WALLET_ADDRESS=your_wallet_address
# PRIVATE_KEY=your_private_key
